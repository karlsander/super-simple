# Music Learning App Code

This folder preserves the rhythm/music learning app source that previously lived in
the main `SuperSimple` iOS target.

It is intentionally source-only for now. It does not include an Xcode project,
target configuration, app icons, signing settings, or CI setup.
